{% extends 'dashboard/base.html.twig' %}

{% block body %}
    <div class="row form-top">
        <div class="col-sm-10">
            <h5>
                {{ project.name }}: {{ 'bank.loan.list' | trans }}
            </h5>
        </div>
        <div class="col-2">
            <a href="{{ path('dashboard_bank_loan_create', { project: project.id }) }}" class="btn-pink btn btn-block">
                {{ 'button.add' | trans | raw }}
            </a>
        </div>
    </div>
    {% include 'dashboard/project/project/view/includes/_tabs.html.twig' with { projectId: project.id } only %}
    <div class="card">
        <div class="card-body">
            {% set pagination = pagination_render(loans) %}
            <div class="table-responsive">
                {% include 'dashboard/bank/loan/loan/_table.html.twig' with { loans: loans, project: project.id } only %}
            </div>
            {% if pagination %}
                {{ pagination | raw }}
            {% endif %}
        </div>
    </div>
{% endblock %}
