{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="d-flex d-row-space form-buttons">
        <button type="submit" class="btn btn-lg btn-pink">
            {{ 'form.validate' | trans }}
        </button>
    </div>
{{ form_end(form) }}
