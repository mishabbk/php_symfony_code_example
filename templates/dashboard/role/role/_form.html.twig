{{ form_start(form) }}
    <div class="row">
        <div class="col-10">
            {{ form_row(form.name) }}
        </div>
        <div class="col-2 mt-3">
            <button type="submit" class="btn btn-pink btn-block">
                {{ 'button.add' | trans }}
            </button>
        </div>
    </div>
{{ form_end(form) }}
